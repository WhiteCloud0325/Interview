描述
现在你有n个数，保存在arr数组中，你需要选出任意两个数求积，问所有这些可能性的和是多少。返回这个和。
返回值对1000000007取模。

样例
Give arr=[1,2,3,4,5] , return 85.

[1,2], 1*2=2
[1,3], 1*3=3
[1,4], 1*4=4
[1,5], 1*5=5
[2,3], 2*3=6
[2,4], 2*4=8
[2,5], 2*5=10
[3,4], 3*4=12
[3,5], 3*5=15
[4,5], 4*5=20
2+3+4+5+6+8+10+12+15+20=85
Give arr=[2,4,4,6,8] , return 220.

[2,4], 2*4=8
[2,4], 2*4=8
[2,6], 2*6=12
[2,8], 2*8=16
[4,4], 4*4=16
[4,6], 4*6=24
[4,8], 4*8=32
[4,6], 4*6=24
[4,8], 4*8=32
[6,8],6*8=48
8+8+12+16+16+24+32+24+32+48=220
int takeTheElementAndQueryTheSum(vector<int> &arr) {
      // Write your code here
      int n = arr.size();
      long long  res = 0;
      long long   sum = 0;
      for (int i = 0; i < n; ++i) {
          sum = (sum + arr[i]) % 1000000007;
      }
      for (int i = 0; i < n - 1; ++i) {
          sum = (sum + 1000000007 - arr[i]) %1000000007;
          res =(res + (sum * arr[i]) % 1000000007) % 1000000007; 
      }
      return res;
  }
